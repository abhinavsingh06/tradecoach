# TradeCoach backend (tradecoach-server). Local dev: ngrok URL or http://localhost:4000
# On a physical phone, localhost won't work — use your machine's LAN IP or ngrok.
EXPO_PUBLIC_API_URL=http://localhost:4000

# Anthropic API key for the AI Coach (optional; Phase 4 will move this server-side).
EXPO_PUBLIC_ANTHROPIC_API_KEY=sk-ant-...
